"""Third-party code vendored verbatim for the bakeoff.

Contents are byte-identical copies of upstream releases, sha-pinned in the
sibling ``provenance.json`` and excluded from ruff (their integrity check is the
recorded digest, not lint). See ``README.md`` for the vendoring rules. Do not
edit vendored files by hand — that breaks the hashes and the frozen-denominator
guarantee.
"""
